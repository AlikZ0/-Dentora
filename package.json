{
  "name": "dentora",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Offline-first local client management PWA (clients, works, x-rays, encrypted backups)",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "node scripts/serve.mjs .output/public 4173",
    "test:e2e": "playwright test",
    "test:all": "vitest run && nuxt generate && playwright test"
  },
  "dependencies": {
    "dexie": "^4.0.10",
    "fflate": "^0.8.2",
    "pinia": "^2.3.0",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@pinia/nuxt": "^0.9.0",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0",
    "@vite-pwa/nuxt": "^0.10.6",
    "fake-indexeddb": "^6.0.0",
    "nuxt": "^3.15.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "vue-tsc": "^2.1.10"
  }
}
